{
  "name": "hookwise-cli",
  "version": "0.1.0",
  "description": "HookWise CLI — forward webhooks to your local server",
  "bin": {
    "hookwise": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^20"
  }
}
